import React from 'react'
import styled from 'styled-components'

const Btn = styled.button`
  color: #fff;
  background-color: var(--accent);
`

const Edit = () => (
  <Btn class='pos-center'>Edit</Btn>
)

export default Edit